# 📝 Notes API

Простой проект для хранения заметок, сделанный на **Go**.  
Использует **PostgreSQL** и **Docker**, чтобы всё запускалось без лишних танцев с бубном.

---

## 🚀 Как запустить

1. Клонируй проект:
   ```bash
   git clone https://github.com/your_username/notes-api.git
   cd notes-api
   ```

2. Создай `.env` файл:
   ```env
   DB_HOST=localhost
   DB_PORT=5432
   DB_USER=postgres
   DB_PASSWORD=yourpassword
   DB_NAME=notesdb
   LISTEN_ADDR=:8080
   ```

3. Запусти проект локально:
   ```bash
   go run main.go
   ```

   или через Docker:
   ```bash
   docker compose up --build
   ```

API будет доступно по адресу 👉 `http://localhost:8080`

---

## 🔥 Примеры запросов

**Создать заметку**
```
POST /notes
{
  "title": "Моя первая заметка",
  "content": "Go — классный язык!"
}
```

**Получить все заметки**
```
GET /notes
```

**Удалить заметку**
```
DELETE /notes/1
```

---

## 📂 Что внутри

```
notes-api/
├── main.go
├── internal/
│   ├── handlers/
│   ├── models/
│   └── storage/
├── migrations/
└── docker-compose.yml
```

---

## 💡 Идея

Это минимальный проект, который можно:
- Развить до полноценного REST API
- Добавить авторизацию, Swagger, тесты и фронт
- Или просто использовать для практики Go + Postgres + Docker

---
